"""
Responsible for loading historical data.
Streaming real time data (prices,quotes,trades,etc.)
map and sanitize the data into standardized format
Also Feed data to the view Dashboard
"""

import re


def loadHistoricalData(source, destination):
    print('source loaded: %s' % source);

def stream(source, destination):
    if state==1:
        print('streaming...');
    elif state==0:
        print('turned off');
    else:
        print('streaming stopping');

def map(source, destination):
    return source

def sanitize(source, destination):
    source = source.format
    return source;

def feedData(source, destination, data):
    return data;



